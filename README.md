# Telecom Customer Churn Prediction

Dette prosjektet bruker IBM sitt Telco Customer Churn-datasett til å bygge
maskinlæringsmodeller i Python som forutser om en kunde kommer til å churn'e
(“forlate” teleoperatøren).

## Hvordan kjøre

1. Opprett og aktiver virtuelt miljø:

```bash
python -m venv .venv
# aktiver venv (se README i repo eller instruksjonene mine)
pip install -r requirements.txt
